// 로그인 페이지 

import React, {useState, useEffect} from 'react';
import styled from 'styled-components';
import Navbar from './Navbar.jsx';

const SignContainer = styled.h1`
    color: white;
`

export default function Sign() {
    return (
        <SignContainer>로그인 !!</SignContainer>
    )
};